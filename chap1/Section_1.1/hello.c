#include <stdio.h> //tells compiler to include information about standard input/output library

main(){ //the () holds the set of arguments; 
    printf("hello, world");  //" "--holds character string; string constant 
// \n is the newline character; must include since printf doesn't provide line advance 
}

/*
Code Notes:
Function: statements that specify the computing operations to be done
Variable: store values used during computation 
Main: special function where program execution begins; EVERY program must have main


 */
